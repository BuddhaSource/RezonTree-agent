servers:
  # Each agent has an RT_AGENT_INDEX mapping to a distinct HD-
  # derived address under the shared RT_AGENT_MNEMONIC. The MCP
  # server derives, signs a WalletLoginIntent, and authenticates
  # via POST /auth/wallet. Backend auto-registers unknown wallets
  # on first login.

  # ── Questioners ─────────────────────────────────────────────────────
  rezontree-questioner-01:
    type: stdio
    command: "npx"
    args: ["tsx", "./mcp-servers/protocol-api/server.ts"]
    description: "RezonTree API for Questioner 01"
    env:
      RT_AGENT_BACKEND_URL: "${RT_AGENT_BACKEND_URL}"
      RT_AGENT_MNEMONIC: "${RT_AGENT_MNEMONIC}"
      RT_AGENT_INDEX: "0"
      RT_ROUTER_ADDRESS: "${RT_ROUTER_ADDRESS}"
      RT_RPC_URL: "${RT_RPC_URL:-https://sepolia.base.org}"
      RT_USDC_ADDRESS: "${RT_USDC_ADDRESS:-0x036CbD53842c5426634e7929541eC2318f3dCF7e}"
      RT_AGENT_DOMAIN_VERIFYING_CONTRACT: "${RT_ROUTER_ADDRESS}"

  rezontree-questioner-02:
    type: stdio
    command: "npx"
    args: ["tsx", "./mcp-servers/protocol-api/server.ts"]
    description: "RezonTree API for Questioner 02"
    env:
      RT_AGENT_BACKEND_URL: "${RT_AGENT_BACKEND_URL}"
      RT_AGENT_MNEMONIC: "${RT_AGENT_MNEMONIC}"
      RT_AGENT_INDEX: "1"
      RT_ROUTER_ADDRESS: "${RT_ROUTER_ADDRESS}"
      RT_RPC_URL: "${RT_RPC_URL:-https://sepolia.base.org}"
      RT_USDC_ADDRESS: "${RT_USDC_ADDRESS:-0x036CbD53842c5426634e7929541eC2318f3dCF7e}"
      RT_AGENT_DOMAIN_VERIFYING_CONTRACT: "${RT_ROUTER_ADDRESS}"

  # ── Solvers (answer + vote) ─────────────────────────────────────────
  rezontree-solver-02:
    type: stdio
    command: "npx"
    args: ["tsx", "./mcp-servers/protocol-api/server.ts"]
    description: "RezonTree API for Solver 02"
    env:
      RT_AGENT_BACKEND_URL: "${RT_AGENT_BACKEND_URL}"
      RT_AGENT_MNEMONIC: "${RT_AGENT_MNEMONIC}"
      RT_AGENT_INDEX: "2"
      RT_ROUTER_ADDRESS: "${RT_ROUTER_ADDRESS}"
      RT_RPC_URL: "${RT_RPC_URL:-https://sepolia.base.org}"
      RT_USDC_ADDRESS: "${RT_USDC_ADDRESS:-0x036CbD53842c5426634e7929541eC2318f3dCF7e}"
      RT_AGENT_DOMAIN_VERIFYING_CONTRACT: "${RT_ROUTER_ADDRESS}"

  rezontree-solver-03:
    type: stdio
    command: "npx"
    args: ["tsx", "./mcp-servers/protocol-api/server.ts"]
    description: "RezonTree API for Solver 03"
    env:
      RT_AGENT_BACKEND_URL: "${RT_AGENT_BACKEND_URL}"
      RT_AGENT_MNEMONIC: "${RT_AGENT_MNEMONIC}"
      RT_AGENT_INDEX: "3"
      RT_ROUTER_ADDRESS: "${RT_ROUTER_ADDRESS}"
      RT_RPC_URL: "${RT_RPC_URL:-https://sepolia.base.org}"
      RT_USDC_ADDRESS: "${RT_USDC_ADDRESS:-0x036CbD53842c5426634e7929541eC2318f3dCF7e}"
      RT_AGENT_DOMAIN_VERIFYING_CONTRACT: "${RT_ROUTER_ADDRESS}"

  rezontree-solver-04:
    type: stdio
    command: "npx"
    args: ["tsx", "./mcp-servers/protocol-api/server.ts"]
    description: "RezonTree API for Solver 04"
    env:
      RT_AGENT_BACKEND_URL: "${RT_AGENT_BACKEND_URL}"
      RT_AGENT_MNEMONIC: "${RT_AGENT_MNEMONIC}"
      RT_AGENT_INDEX: "4"
      RT_ROUTER_ADDRESS: "${RT_ROUTER_ADDRESS}"
      RT_RPC_URL: "${RT_RPC_URL:-https://sepolia.base.org}"
      RT_USDC_ADDRESS: "${RT_USDC_ADDRESS:-0x036CbD53842c5426634e7929541eC2318f3dCF7e}"
      RT_AGENT_DOMAIN_VERIFYING_CONTRACT: "${RT_ROUTER_ADDRESS}"

  rezontree-solver-05:
    type: stdio
    command: "npx"
    args: ["tsx", "./mcp-servers/protocol-api/server.ts"]
    description: "RezonTree API for Solver 05"
    env:
      RT_AGENT_BACKEND_URL: "${RT_AGENT_BACKEND_URL}"
      RT_AGENT_MNEMONIC: "${RT_AGENT_MNEMONIC}"
      RT_AGENT_INDEX: "5"
      RT_ROUTER_ADDRESS: "${RT_ROUTER_ADDRESS}"
      RT_RPC_URL: "${RT_RPC_URL:-https://sepolia.base.org}"
      RT_USDC_ADDRESS: "${RT_USDC_ADDRESS:-0x036CbD53842c5426634e7929541eC2318f3dCF7e}"
      RT_AGENT_DOMAIN_VERIFYING_CONTRACT: "${RT_ROUTER_ADDRESS}"

  # ── Standard MCP servers ────────────────────────────────────────────
  filesystem:
    type: stdio
    command: "npx"
    args: ["-y", "@modelcontextprotocol/server-filesystem", "./workspace"]
    description: "Read/write files in workspace directory"

  github:
    type: stdio
    command: "npx"
    args: ["-y", "@modelcontextprotocol/server-github"]
    env:
      GITHUB_TOKEN: "${GITHUB_TOKEN}"
    description: "GitHub repository operations"
